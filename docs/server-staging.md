# Серверная тестовая версия

23.09.2026: наполнены30 карточек из двух выгрузок WB, всего35 товаров. Backup до наполнения — `/srv/autoreelz2026/backups/before-wb-catalog-20260923T203959Z.dump`. Фото и данные в CMS; остатки перенесены только из колонки продавца. Для вертикальных фото выпущена CSS-правка256d81aa85d91987e5dc284e9d28c4c8f93209f6. Миграций нет; проверка — artifacts/wb-catalog. При откате приложения каталог сохраняется.

Разрешена владельцем 23.09.2026: основной `autoreelz.ru`, сервер `91.200.150.79`; SSH root:22 с новым ключом из private. Timeweb Pro CRT/цепочка/ключ предоставлены владельцем и проверены: SAN autoreelz.ru/www.autoreelz.ru, срок до 10.03.2027, ключ совпадает. Сертификат не перевыпускается.

## Размещение

Сервер Ubuntu22.04.5 x86_64,4CPU/8 ГБ RAM/79 ГБ диск (54 ГБ свободно при проверке), ISPmanager. Nginx/Apache/почта уже работают. Для нового магазина создан отдельный пользователь `autoreelz2026` (UID1018), каталог `/srv/autoreelz2026` и rootless Docker29.8.1 с cgroupv2. Compose5.5.1 и Buildx0.37.1 установлены только этому пользователю; бинарники проверены по опубликованным SHA256. Из apt добавлены только uidmap/slirp4netns/libslirp0, без обновлений/удалений прежних пакетов и перезапуска прежних служб.

Compose-проект `autoreelz2026-release`, файлы `infra/release/compose.yaml` + `ispmanager.yaml`. Edge слушает только 127.0.0.1:14324, внешний HTTPS остаётся за Nginx. Витрина открыта без общего пароля по последующему поручению владельца23.09.2026; noindex и тестовый режим сохраняются. `/cms/admin` обслуживает Directus с собственным входом: двойной Basic Auth мешает Bearer API Studio, поэтому для `/cms/*` используется штатная авторизация CMS. БД/CMS/web не публикуют отдельные порты.

`STORE_MODE=staging` разрешает только тестовые адаптеры на точном HTTPS-origin `https://autoreelz.ru`; production по-прежнему их блокирует. Поиск закрыт от индексации даже при случайном SEO_INDEXING_ENABLED=true. На сервере включена yookassa-sandbox: карта на странице ЮKassa, сохраняемая попытка и worker-сверка. Полная тестовая оплата4900.00RUB прошла; реальные деньги и сообщения выключены.

## Данные и доступы

Файловый доступ по SFTP:91.200.150.79,порт 22,пользовательroot, ключ локального проекта `private/ssh_server_ed25519` (без .pub). Корень магазина `/srv/autoreelz2026/`; исходники — `releases/256d81aa85d91987e5dc284e9d28c4c8f93209f6/`, конфигурация/перенос — `shared/`. Изменение исходника через SFTP само по себе не меняет работающий Docker-образ: нужна проверенная сборка и замена web/worker.

Живые файлы CMS находятся в rootless-томе `/srv/autoreelz2026/.local/share/docker/volumes/autoreelz2026-release_uploads/_data`. Обычно добавлять их через Directus: он создаёт нужную запись/метаданные и связь с товаром. `shared/bootstrap/uploads` — первоначальная копия, не рабочий каталог медиа.

Отдельно по просьбе владельца опубликован статический тур `/muzey/` без пароля. Его файлы — `/srv/autoreelz2026/public/muzey/`, доступны для редактирования по SFTP без пересборки Astro. Подробности и проверки — [muzey.md](muzey.md).

Согласованная копия берётся только из нового локального проекта. `scripts/prepare-staging-copy.mjs` работает с удаляемой QA-базой: сохраняет каталог/контент/CMS-метаданные, очищает сессии/OTP/тестовые заказы/активность, меняет пароли сотрудников и отключает старую лицензионную привязку в копии. Локальная БД не меняется. Все новые секреты, TLS и файлы доступа остаются в private и вне Git.

Локальный архив переноса: `private/staging-transfer-2026-09-23T15-04-22-391Z/`. Новая привязка OIG — `https://autoreelz.ru/cms`; исходная локальная лицензия не деактивируется. На сервере ключ применять только к новому Directus, без повторной активации одноразовых клонов.

## Переключение и откат

Владелец подтвердил, что сам удалил прежний сайт из ISPmanager во время работы. Первоначальный vhost исчез до нашего переключения и нами не изменялся. Новый маршрут создан отдельно: `/etc/nginx/conf.d/autoreelz2026.conf`. Перед установкой проверено отсутствие другой конфигурации домена; nginx -t прошёл, reload выполнен без остановки служб. Остальные vhosts не менялись. Повторное добавление autoreelz.ru как обычного сайта ISPmanager может создать конфликт маршрутов; этим магазином управлять через его Compose и отдельную конфигурацию.

Откат маршрута: убрать только наш conf.d/autoreelz2026.conf после сохранения его копии, выполнить nginx -t и reload. Прежний сайт владелец удалил самостоятельно, поэтому его восстановление не является автоматическим откатом. Для отката приложения использовать предыдущий совместимый image digest, не перезаписывать БД. TLS-файлы нового домена находятся отдельно в `/etc/ssl/autoreelz2026`; другие сертификаты не изменялись.

## Фактический результат

Сайт работает: https://autoreelz.ru; CMS: https://autoreelz.ru/cms/admin; менеджер: https://autoreelz.ru/manager. Данные входа — локальный `private/server-access.md` (0600, вне Git). Витрина открыта; CMS и менеджер требуют собственного входа. Тема CMS светлая.

Текущий app SHA: `256d81aa85d91987e5dc284e9d28c4c8f93209f6`; серверный image ID: `sha256:1f51b568560794aba66ed453761d68a88175e0454c6de4bcfece9f01eef54226`. Проверены HTTP health и OCI-label. Runtime-конфигурация/отчёты фиксируются последующим инфраструктурным коммитом; это не другой бинарный образ приложения. Локальная Docker-витрина остаётся отдельной средой.

DB/CMS/web/worker healthy; edge отвечает на HTTP-проверки. Ограничения web реально применены:1 ГБ/2CPU. Для этого добавлено делегирование контроллеров только новому user@1018.service; его Docker перезапущен, прежние службы не останавливались. Применены миграции 001–010; при первоначальном переносе сохранены 4 демотовара/7SKU, старые тестовые заказы/сессии исключены. OIG active,37 коллекций/2 пользователя; локальная OIG также сохранена.

Публичные проверки: HTTPS с обычной валидацией сертификата;401 без пароля/noindex; точный SHA; выбранный SKU4900.00RUB и медиа при 375/1440px; вход Studio и видимые 4 строки товаров; вход менеджера. Ошибок JS не зафиксировано. `artifacts/staging/public-check.json`, `public-cms-check.json`, снимки и `deployment.json`. Первоначальный снимок CMS попал на переходный загрузочный экран; контроль дополнен ожиданием реально видимой строки товара и повторён.

Внешний HTTPS проходит через DDoS-Guard, который предъявляет свой валидный сертификат. На origin Nginx установлен предоставленный Timeweb Pro до 10.03.2027, соответствие ключа/цепочки/SAN проверено OpenSSL. Автопродление в кабинете Timeweb не настраивалось и не выключалось; автоматическая доставка следующего CRT/KEY на этот VPS пока не реализована.

Начальный архив переноса сохранён на сервере в `/srv/autoreelz2026/shared/bootstrap`. Не выполнять первоначальный импорт повторно: после начала наполнения серверная БД — рабочий источник данных. Регулярные серверные/offsite-копии и мониторинг с внешними уведомлениями остаются частью этапа 7. Тестовая ЮKassa подключена; СДЭК проверен только чтением (OAuth и ПВЗ отправления), калькулятор ещё не включён. Рабочие платежи/email/MAX и платная генерация не включены.

## Обновление тестовой оплаты 23.09.2026

Перед010 создана серверная копия `/srv/autoreelz2026/backups/before-yookassa-010-20260923T165806Z.dump` (280071 байт,600); архив читается pg_restore. Контрольные суммы001–009 совпали, данные не импортировались повторно. Настройки сервера сохранены рядом в `shared/bootstrap/server.env.before-yookassa-*`; изменены только RELEASE_IMAGE/PAYMENT_PROVIDER и два поля ЮKassa. Ключи интеграций — `shared/integrations`,0600, вне исходников/образа/Git. СДЭК: MKHCH13/город442, принимает отправления.

Обновлены web/worker/edge, CMS перечитала схему; настроены13 новых полей платежей только для чтения, контент и ссылки владельца сохранены. DB/CMS/web/worker healthy. Exact webhook `/api/payments/yookassa` открыт без Basic; остальные торговые API закрыты. `/muzey/` по-прежнему200 без пароля. Неверный webhook400, большое тело413, неизвестный ID200 без создания магазинной сессии. Cookies DDoS-Guard допустимы.

Реальная sandbox-проверка: заказ AR-26-010002,4900.00RUB, официальная тестовая карта; ЮKassa succeeded, возврат на сайт, worker перевёл заказ в paid. В БД ровно1 попытка/событие/списание. Демотовар возвращён складской командой с причиной; оплаченный QA-заказ сохранён для истории, отправлять его нельзя. Результаты/375 и1440px — `artifacts/yookassa/public-*`, PSP-экран — `provider-test-result.png`.

Владелец сохранил подписку HTTP-уведомлений в тестовом кабинете. Второй заказ AR-26-010003 оплачен тестовой картой при полностью остановленном worker: статус paid появился через настоящий webhook. Worker остановлен17:15:42UTC, подтверждение до17:16:19UTC, запущен17:16:25UTC. Демотовар снова восстановлен командой склада; оба QA-заказа не отправлять. Доказательства — artifacts/yookassa/webhook-isolation-check.json и webhook-db-check.json. API-сверка остаётся резервным способом. После первой ЮKassa-попытки **не откатывать на старую6ab6bea** без блокировки POST оплаты и остановки worker: в старом коде нет проверки provider. Применённые миграции и платёжные записи сохранять; предпочтителен исправленный новый выпуск.

## СДЭК, Яндекс и открытие витрины

Владелец разрешил публичную витрину и предоставил ключ JavaScript API Яндекса. Общий Basic Auth удалён только из storefront-handle нового Caddy; /cms и /manager сохраняют штатную авторизацию. Проверено: / →200 без пароля, /api/manager/orders и /cms/users/me →401 без входа, /muzey/ →200, noindex сохраняется. Caddy admin API отключён: после проверки конфигурации применять её перезапуском только edge нового проекта, не через caddy reload.

Окружение web: SHIPPING_PROVIDER=cdek, PICKUP_MAP_PROVIDER=yandex. Приватные файлы — shared/integrations/cdek.env и yandex-maps.env; остальные серверные секреты сохранены. Перед обновлением создана копия /srv/autoreelz2026/backups/before-cdek-checkout-20260923T181009Z.dump; pg_restore --list прошёл. Новых миграций нет, БД не импортировалась повторно.

Сквозной QA-заказ AR-26-010004:1800.00+390.00=2190.00RUB, ПВЗ MSK2, тариф136; тестовая ЮKassa подтвердила оплату. Остаток товара владельца восстановлен командой склада до10шт.; этот QA-заказ не отправлять. Доказательства — artifacts/cdek-checkout/public-payment-check.json. Позже добавлен ранний расчёт доставки при выборе ПВЗ без контактов/резерва; публичная проверка — public-estimate-check.json.

При откате сохранять открытый storefront-handle. Версии до подключения СДЭК возвращают имитацию доставки: предварительно закрыть оформление и оценить совместимость. Не откатывать ниже440ac82 с существующими ЮKassa-попытками. Регулярные offsite/мониторинг и реальные отправления остаются следующей работой.

## Корзина и подтверждения — 23.09.2026

Выпущен app `3e4c2bc16f8bbbebc050cec8854d3b4104e3f77a`: автосохранение количества, русская проверка формы, подтверждение тестовых условий checkout, отдельное согласие перед загрузкой Яндекс.Карт, /cookies. Карта остаётся основным интерфейсом выбора ПВЗ.

Добавлена миграция011: неизменяемое confirmation в ar_checkout_batches и служебная ar_service_consents. Старые batch остаются NULL, согласия им не приписываются. Не давать CMS-редактору право изменять этот аудит. Версию текста менять при каждом смысловом изменении src/lib/checkout-confirmations.ts. Окончательная оферта не утверждена; механизм пока подтверждает явно обозначенные тестовые условия.

Backup: `/srv/autoreelz2026/backups/before-checkout-confirmations-20260923T201713Z.dump`, 0600; pg_restore --list проверен. Обновлена только RELEASE_IMAGE в сохранённом серверном окружении, пересозданы web/worker/edge. БД/CMS/старые службы не пересоздавались. Публичный доступ, /muzey/, CDEK, Yandex и sandbox сохранены. Новых заказов публичный smoke не создавал. Проверка/образ — artifacts/checkout-ux/deployment.json и public-check.json.

Миграция добавочная; откат приложения к a908f83 сохраняет БД/оплаты, но уберёт новые подтверждения и защиту от загрузки карты до согласия. Откат БД/повторный начальный импорт не выполнять.

## Удаление страницы cookies — 23.09.2026

По поручению владельца опубликован app `5dcbe1efd92020f2c465746bb0bede81bb6b7dda`: удалены маршрут /cookies, уведомление и ссылка на него. /cookies и /cookies/ возвращают404, основные страницы и /muzey/200. Подтверждения checkout/Яндекса, механика cookies и БД не менялись; миграций нет. Образ/проверка — artifacts/remove-cookies/public-check.json.
