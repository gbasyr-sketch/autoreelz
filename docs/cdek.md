# СДЭК и выбор ПВЗ

Состояние на24.09.2026: **реальные справочники и калькулятор подключены к публичному checkout**. Создание отправлений, этикеток и вызов курьера не реализованы. [Контракт](cdek-checkout-contract.md) · [Текущее состояние](status.md).

## Настройки

Договор/API предоставлены владельцем. `private/cdek.env` содержит CDEK_CLIENT_ID/CDEK_CLIENT_SECRET и отправителя; не печатать значения. Файл не подхватывается web автоматически: в Compose должны быть переданы соответствующие runtime-переменные.

Отправитель: MKHCH13, Махачкала, пр-т Петра Первого,83; город442. Это место отправления, а не согласованный юридический адрес/адрес возвратов.

## Рабочий сценарий

Город/регион → пункт на карте/в списке либо адрес курьера → предварительная цена без контактов → полный quote → отдельные заказы для наличия/предзаказа. Тарифы136/137; стоимость приходит из СДЭК, клиентский price не доверяется. Смена выбора очищает старую сумму; запоздалый ответ не заменяет новый. Quote живёт5минут.

Яндекс.Карты выбраны владельцем как основной интерфейс. SDK загружается после отдельного согласия; ошибка карты не ломает список ПВЗ. `PICKUP_MAP_PROVIDER=yandex`, ключ в `private/yandex-maps.env`/серверном env. OSM/Leaflet сохранён для локальной проверки/иной явно выбранной конфигурации, не как текущий серверный провайдер.

Ключ Яндекс JS API штатно виден браузеру; ограничения Referer задаются в кабинете. Сеть/SDK иногда давали ERR_SOCKET_NOT_CONNECTED в автоматическом Chrome. Это ограничение конкретной проверки, а не основание незаметно менять рабочий loader. Эксперимент в private не опубликован. Подложку Яндекса маскировать в сохраняемых QA-снимках.

## Упаковка

БД хранит массу в граммах, размеры в миллиметрах; клиент переводит размеры в см для СДЭК. Масса/коробка одного SKU не распространяется автоматически на2шт./комплект. Если нет точного правила или API не ответил — стоимость уточняет менеджер до оплаты, не ноль.

Первый артикул «гранта зеленая»: цена1800₽ и исходный приход10шт. предоставлены владельцем; упаковка200г,220×100×40мм. Два фото позже добавлены из WB. Остаток динамический, старое число10 не восстанавливать. После [импорта WB](wb-catalog-import.md) добавлены30 упаковок единичных SKU, но не правила всех составов.

## Проверки и следующий шаг

Протокол — tests/cdek.test.ts; транзакционный сценарий — tests/cdek-checkout.test.ts; реальный справочник/калькулятор в QA — tests/cdek-browser.mjs. Флаги/порты — [testing.md](testing.md). Отчёты — [cdek-checkout](../artifacts/cdek-checkout).

Исторические проверки дали Москва390₽, курьер640₽, MKHCH13→MKHCH13 185₽ для конкретной упаковки. Это не фиксированные тарифы. Полный QA-платёж с доставкой прошёл через ЮKassa sandbox; настоящей отправки не было.

Дальше B04/B09: упаковки нескольких штук, создание/отмена отправления, tracking/events и ручные исключения. Условия API сверять по [официальному порталу](https://apidoc.cdek.ru/) на дату реализации; новую отправку не создавать диагностическим запросом без поручения.
