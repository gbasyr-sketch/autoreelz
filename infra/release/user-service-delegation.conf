# Installed as /etc/systemd/system/user@1018.service.d/autoreelz-delegate.conf on this host.
# Scope only the dedicated deployment user, not the global user@.service template.
[Service]
Delegate=cpu cpuset io memory pids
